{
  "name": "rsbuild-cloudflare-example-basic-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "build:default": "rsbuild build",
    "check:type": "tsc --build",
    "deploy:dry-run": "pnpm build && wrangler deploy --dry-run",
    "dev": "rsbuild dev --host 127.0.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260603.1",
    "@rsbuild/core": "2.0.9",
    "rsbuild-cloudflare": "workspace:*",
    "typescript": "5.8.3",
    "wrangler": "^4.98.0"
  }
}
